name: Email Build
on:
  push:
    branches:
      - main
  pull_request:
jobs:
  email-build:
    runs-on: depot-ubuntu-22.04-2
    container:
      image: node:24
    steps:
      - name: Checkout
        uses: actions/checkout@v6
      - name: pnpm setup
        uses: pnpm/action-setup@v4
        with:
          version: 10.23.0
      - name: Install packages
        run: pnpm install
      - name: Run React Email Build
        run: pnpm email:build
